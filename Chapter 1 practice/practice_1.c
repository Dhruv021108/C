#include <stdio.h>

int main() {
    int length = 20;  // Setting length to 10
    int breadth = 50; // Setting breadth to 10

    printf("The area of this rectangle is %d", length * breadth);  // Calculating and printing area

    return 0;
}
